#!/bin/sh
set -eu

GRUB_DIR="${LB_BINARY:-binary}/boot/grub"

if [ ! -f "$GRUB_DIR/splash.png" ]; then
    echo "ERROR: AlphaOS GRUB splash.png missing."
    exit 1
fi

echo "AlphaOS: GRUB splash installed."
