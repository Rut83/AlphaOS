#!/bin/sh
set -eu

mkdir -p /etc/alphaos

cat > /etc/alphaos/live-session.conf <<'EOL'
DESKTOP=kde
DISPLAY_MANAGER=sddm
EOL

mkdir -p /etc/sddm.conf.d

cat > /etc/sddm.conf.d/10-alphaos.conf <<'EOL'
[Autologin]
User=alpha
Session=plasma.desktop
Relogin=false
EOL

rm -rf /etc/lightdm

